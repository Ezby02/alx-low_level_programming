#include <stdio.h>
#include "main.h"
/**
 * main-entry point
 *
 * Print alphabet in lowercase
 *
 * Return: Always 0
 */
int main(void)
{
void print_alphabet(void);
{
	print_alphabet();
	char letter;

	for (letter = 'a'; letter <= 'z'; letter++)
		_putchar(letter);

	_putchar("\n");
	return (0);
}
}
